name: numbers_to_nepali_word_converter
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/UmeshChakre/nepali_numbers_utils
homepage: https://github.com/UmeshChakre

environment:
  sdk: ^3.2.5

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.1.0
  test: ^1.24.0
